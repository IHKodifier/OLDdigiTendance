import 'package:flutter/material.dart';

class StudentAppHome extends StatelessWidget {
  const StudentAppHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('student home'));
  }
}
