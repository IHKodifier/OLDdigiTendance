import 'package:flutter/material.dart';

class TeacherAppHome extends StatelessWidget {
  const TeacherAppHome({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('teacher Home'),
      
    );
  }
}